import {useState} from 'react'; import {useNavigate,Link} from 'react-router-dom'; import {api} from '../services/api';
export default function Register(){const [f,setF]=useState({email:'',full_name:'',password:''}),[error,setError]=useState('');const nav=useNavigate();return <div className="auth-card"><div className="auth-brand"><span className="brand-mark">▣</span><div><strong>Research Intelligence</strong><small>Discover. Analyze. Innovate.</small></div></div><h1>Create your account</h1><p className="subtitle">Start building your personalized research intelligence workspace.</p><form onSubmit={async e=>{e.preventDefault();setError('');try{await api('/auth/register',{method:'POST',body:JSON.stringify(f)});nav('/login')}catch(err){setError(err.message)}}}><label>Full name<input required value={f.full_name} onChange={e=>setF({...f,full_name:e.target.value})} placeholder="Your full name"/></label><label>Email<input type="email" required value={f.email} onChange={e=>setF({...f,email:e.target.value})} placeholder="you@example.com"/></label><label>Password<input type="password" minLength="8" required value={f.password} onChange={e=>setF({...f,password:e.target.value})} placeholder="At least 8 characters"/></label>{error&&<p className="error">{error}</p>}<button className="button">Create account</button></form><p className="muted" style={{textAlign:'center',marginBottom:0}}>Already registered? <Link to="/login" style={{color:'#1769e0',fontWeight:700}}>Sign in</Link></p></div>}
