import {useState} from 'react'; import {useNavigate} from 'react-router-dom'; import {api} from '../services/api';
export default function Register(){const [f,setF]=useState({email:'',full_name:'',password:''}),[error,setError]=useState('');const nav=useNavigate();return <section className="card narrow"><h2>Create account</h2><form onSubmit={async e=>{e.preventDefault();setError('');try{await api('/auth/register',{method:'POST',body:JSON.stringify(f)});nav('/login')}catch(err){setError(err.message)}}}><label>Full name<input required value={f.full_name} onChange={e=>setF({...f,full_name:e.target.value})}/></label><label>Email<input type="email" required value={f.email} onChange={e=>setF({...f,email:e.target.value})}/></label><label>Password<input type="password" minLength="8" required value={f.password} onChange={e=>setF({...f,password:e.target.value})}/></label>{error&&<p className="error">{error}</p>}<button className="button">Register</button></form></section>}
