import {useState} from 'react'; import {useNavigate,Link} from 'react-router-dom'; import {useAuth} from '../context/AuthContext';
export default function Login(){const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState('');const {login}=useAuth();const nav=useNavigate();return <div className="auth-card"><div className="auth-brand"><span className="brand-mark">▣</span><div><strong>Research Intelligence</strong><small>Discover. Analyze. Innovate.</small></div></div><h1>Welcome back</h1><p className="subtitle">Sign in to continue to your research workspace.</p><form onSubmit={async e=>{e.preventDefault();setError('');try{await login(email,password);nav('/dashboard')}catch(err){setError(err.message)}}}><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required placeholder="you@example.com"/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required placeholder="••••••••"/></label>{error&&<p className="error">{error}</p>}<button className="button">Sign in</button></form><p className="muted" style={{textAlign:'center',marginBottom:0}}>New here? <Link to="/register" style={{color:'#1769e0',fontWeight:700}}>Create an account</Link></p></div>}
